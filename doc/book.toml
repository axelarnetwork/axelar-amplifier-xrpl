[book]
language = "en"
src = "src"
title = "Amplifier Documentation"

[build]
build-dir = "../target/book"
create-missing = false

[preprocessor]

[preprocessor.mermaid]
command = "mdbook-mermaid"

[output.html]

# linkcheck disabled: mdbook-linkcheck v0.7.7 is incompatible with the
# current mdbook RenderContext schema. Re-enable once upstream catches up,
# or run a separate link checker (e.g. `lychee doc/src/**/*.md`).
# [output.linkcheck]
# warning-policy = "error"
# traverse-parent-directories = true
